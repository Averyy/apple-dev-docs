# ALL versions pinned exactly: this image is rebuilt automatically (scheduled
# scrapes + CI cache evictions), so unpinned deps can silently jump major
# versions between builds. Bump deliberately and test locally first
# (docker-compose.local.yml). Shared packages must match the root
# requirements.txt — both files install into the same Docker image.

# Core dependencies
meilisearch==0.42.0   # Search engine client (keep matched to meilisearch server v1.49.0 in Dockerfile)
python-dotenv==1.2.2  # Environment management
httpx==0.28.1         # Async HTTP client
tqdm==4.68.4          # Progress bars for indexing

# MCP SDK and FastMCP framework
# Pinned: unpinned fastmcp pulled in 3.x on 2026-07-07 and its new Host header
# guard (421 Misdirected Request) took down xdocs.dev. Upgrade deliberately.
mcp==1.28.1           # Model Context Protocol SDK
fastmcp==3.4.3        # FastMCP standalone framework (3.x needs allowed_hosts in http_app)

# ASGI server and middleware
uvicorn[standard]==0.50.2  # ASGI server
starlette==1.3.1      # ASGI framework (for middleware)
pydantic==2.13.4      # Data validation

# Additional dependencies
beautifulsoup4==4.12.3
markdownify==0.12.1   # 1.x changes markdown output — would churn scraped docs
asyncio-throttle==1.0.2
jsonschema==4.21.1
structlog==24.1.0
rich==15.0.0
