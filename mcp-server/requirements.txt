# Core dependencies only
chromadb>=0.4.0       # Vector database (REQUIRED for RAG)
openai                # OpenAI API for embeddings (REQUIRED)
python-dotenv         # Environment management
httpx==0.27.0         # Async HTTP client
tqdm                  # Progress bars for indexing

# MCP SDK (REQUIRED for server)
mcp[fastmcp]>=1.3.0   # Model Context Protocol SDK with FastMCP framework
uvicorn[standard]     # ASGI server for FastMCP
pydantic>=2.0         # Data validation for FastMCP

# Scraper dependencies (for scheduled rescraping)
beautifulsoup4==4.12.3
markdownify==0.12.1
asyncio-throttle==1.0.2
jsonschema==4.21.1
tiktoken>=0.5.0
structlog==24.1.0
rich==13.7.0