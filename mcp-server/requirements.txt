# Core dependencies
meilisearch==0.36.0   # Search engine client
python-dotenv         # Environment management
httpx>=0.27.1         # Async HTTP client (mcp requires >=0.27.1)
tqdm                  # Progress bars for indexing

# MCP SDK and FastMCP framework
mcp>=1.3.0            # Model Context Protocol SDK
fastmcp>=2.0.0        # FastMCP standalone framework

# ASGI server and middleware
uvicorn[standard]     # ASGI server
starlette             # ASGI framework (for middleware)
pydantic>=2.0         # Data validation

# Additional dependencies
beautifulsoup4==4.12.3
markdownify==0.12.1
asyncio-throttle==1.0.2
jsonschema==4.21.1
structlog==24.1.0
rich>=13.9.4
