# configuration

**Framework**: CKTool JS  
**Kind**: property

A `Configuration` instance returned from `createConfiguration`.

**Availability**:
- CKTool JS 1.2.15+

## Declaration

```swift
attribute Configuration configuration;
```

#### Discussion

A `createConfiguration` function from the `@apple/cktool.target.nodejs` package creates an instance of `Configuration` appropriate for Node.js.


---

*[View on Apple Developer](https://developer.apple.com/documentation/cktooljs/promisesapioptions/configuration)*