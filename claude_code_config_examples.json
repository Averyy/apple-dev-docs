{
  "comment": "MCP Configuration Examples for Apple Docs",
  "comment2": "Choose the appropriate configuration based on your client's transport support",

  "http_clients": {
    "description": "For clients supporting Streamable HTTP (Claude Code, Cursor, Windsurf)",
    "config": {
      "apple-docs": {
        "type": "http",
        "url": "https://xdocs.dev/mcp"
      }
    }
  },

  "stdio_clients": {
    "description": "For STDIO-only clients, use mcp-remote as a proxy",
    "config": {
      "apple-docs": {
        "command": "npx",
        "args": ["-y", "mcp-remote", "https://xdocs.dev/mcp"]
      }
    }
  },

  "self_hosted": {
    "description": "For self-hosted servers, replace the URL",
    "http_config": {
      "apple-docs": {
        "type": "http",
        "url": "https://your-server.com/mcp"
      }
    },
    "stdio_config": {
      "apple-docs": {
        "command": "npx",
        "args": ["-y", "mcp-remote", "https://your-server.com/mcp"]
      }
    }
  }
}
